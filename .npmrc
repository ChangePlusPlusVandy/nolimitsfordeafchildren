# Ensure optional dependencies are installed correctly
# This fixes issues on Windows with packages like @rollup/rollup-win32-x64-msvc
optional=true
auto-install-peers=true

# Use legacy peer deps if needed for compatibility
# legacy-peer-deps=false

# Ensure proper installation of platform-specific binaries
prefer-offline=false

